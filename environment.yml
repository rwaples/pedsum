name: pedsum
channels:
  - conda-forge
dependencies:
  - python>=3.13
  - numpy
  - scipy
  - pandas
  - pyyaml
  - numba
  # - pigz  # optional: ~3× faster .tsv.gz writes; pedsum falls back to gzip if missing
  - pytest
  - hypothesis
  - ruff
  - pip
  - pip:
      - pedigree-graph @ git+https://github.com/rwaples/pedigree-graph.git@v0.5.2
